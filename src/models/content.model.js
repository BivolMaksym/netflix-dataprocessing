class Content {
    constructor(contentId, movieId, seriesId, profileId) {
        this.movieId = movieId;
        this.contentId = contentId;
        this.seriesId = seriesId;
        this.profileId = profileId;
    }
}

module.exports = Content;